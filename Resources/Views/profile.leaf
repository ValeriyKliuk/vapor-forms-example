#extend("base")

#export("title") { Edit Profile }

#export("content") {
  <form action="" method="POST" class="form-horizontal">
    <div class="form-group #if(errors.name) { has-error }">
      <label for="name" class="col-sm-2 control-label">Name</label>
      <div class="col-sm-10">
        <input type="text" name="name" value="#(form.name)" class="form-control" />
        #if(errors.name) { <span class="help-block">#(errors.name)</span> }
      </div>
    </div>

    <div class="form-group #if(errors.email) { has-error }">
      <label for="email" class="col-sm-2 control-label">Email</label>
      <div class="col-sm-10">
        <input type="text" name="email" value="#(form.email)" class="form-control" />
        #if(errors.email) { <span class="help-block">#(errors.email)</span> }
      </div>
    </div>

    <div class="form-group #if(errors.avatar_url) { has-error }">
      <label for="avatar_url" class="col-sm-2 control-label">Avatar URL</label>
      <div class="col-sm-10">
        <input type="text" name="avatar_url" value="#(form.avatar_url)" class="form-control" />
        #if(errors.avatar_url) { <span class="help-block">#(errors.avatar_url)</span> }
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <button type="submit" class="btn btn-primary">Save</button>
      </div>
    </div>
  </form>
}
